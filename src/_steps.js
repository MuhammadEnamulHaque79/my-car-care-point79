/*
1.Create a project into firebase;
2.Register app;
3.npm install firebase(yarn add firebase);
4.Initialized firebase into firebase.init.js;const auth=getAuth(app);import {getAuth} from "firebase/auth" and export default auth;
5.Authentication enable for desire provider;
6.

 */

// firebase hosting steps;
/*
 1.npm install -g firebase-tools;
 2.firebase login();
 3.firebase init();
 4.npm run build(yarn build)//every time before deploy;
 4.firebase deploy(every time to deploy); 
 */
// react-router add process;
/*
1.yarn add react-router@6;
2.import BrowserRouter into index.js from 'react-router-dom';
3.import Routes,Route into app.js from 'react-router-dom';
4.
 */